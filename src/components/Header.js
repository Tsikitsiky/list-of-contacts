import { HStack, Text } from '@chakra-ui/layout'

const Header = () => {
  return (
    <HStack justifyContent='center' paddingBlock='16px' backgroundColor=''>
      <Text>Contacts</Text>
    </HStack>
  )
}

export default Header
